{
    "rules": {
        "status": {
            ".read": "auth.token.role === 'ADMIN'",
            "$uid": {
                ".read": "$uid === auth.uid || auth.token.role === 'ADMIN'",
                ".write": false
            }
        }
    }
}