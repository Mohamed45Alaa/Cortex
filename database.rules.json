{
    "rules": {
        "status": {
            ".read": "auth.token.role === 'ADMIN'",
            "$uid": {
                ".read": "$uid === auth.uid",
                ".write": "$uid === auth.uid",
                ".validate": "newData.hasChildren(['state', 'lastChanged'])"
            }
        }
    }
}